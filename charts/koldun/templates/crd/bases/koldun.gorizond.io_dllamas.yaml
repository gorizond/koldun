---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: dllamas.koldun.gorizond.io
spec:
  group: koldun.gorizond.io
  names:
    kind: Dllama
    listKind: DllamaList
    plural: dllamas
    shortNames:
    - dll
    singular: dllama
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Dllama describes a distributed-llama topology managed by the
          operator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DllamaSpec captures the desired state for a distributed-llama
              deployment.
            properties:
              cacheSpec:
                description: CacheSpec describes optional cache service configuration
                  for model artifacts.
                properties:
                  bucket:
                    description: Bucket is the object store bucket where models are
                      stored.
                    type: string
                  endpoint:
                    description: Endpoint is the URL of the cache backend (e.g. S3
                      compatible object storage).
                    type: string
                  secretRef:
                    description: SecretRef references a Secret containing credentials
                      for the cache backend.
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                required:
                - bucket
                - endpoint
                type: object
              launchArgs:
                description: LaunchArgs are appended to distributed-llama launch command
                  for both root and workers.
                items:
                  type: string
                type: array
              modelRef:
                description: ModelRef references the model artifact that should be
                  available on the cluster nodes.
                type: string
              replicaPower:
                description: ReplicaPower is the exponent that defines the worker
                  fan-out (totalNodes = 2^ReplicaPower).
                format: int32
                type: integer
              rootImage:
                description: RootImage is the container image used for the root coordinator
                  process.
                type: string
              workerImage:
                description: WorkerImage is the container image used for the worker
                  processes.
                type: string
            required:
            - modelRef
            - replicaPower
            - rootImage
            - workerImage
            type: object
          status:
            description: DllamaStatus provides high-level status for the distributed-llama
              deployment.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              readyRoot:
                description: ReadyRoot indicates whether the root pod is reported
                  as ready.
                type: boolean
              readyWorkers:
                description: ReadyWorkers reflects how many workers are currently
                  reported as ready.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
