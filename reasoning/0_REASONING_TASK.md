# Koldun Platform - Итеративная отладка и развитие

## ⚠️ КРИТИЧЕСКИ ВАЖНО - Управление контекстом LLM

### Для ЛЮБОЙ LLM (Claude Code, ChatGPT, Claude API, и т.д.)

**ВСЕГДА ПРИКРЕПЛЯЙТЕ ТОЛЬКО ЭТОТ ФАЙЛ (`reasoning/0_REASONING_TASK.md`) В КОНТЕКСТ!**

**❌ НИКОГДА НЕ ПРИКРЕПЛЯЙТЕ ФАЙЛЫ СЕССИЙ:**
- ❌ `iterations/2025-11-01_01-20_session_2.md`
- ❌ `iterations/YYYY-MM-DD_HH-MM_session_N.md`
- ❌ Любые другие файлы из папки `iterations/`

**Почему это критично:**
1. **Файлы сессий огромные** - быстро забивают token budget (часто >50000 токенов)
2. **Мастер-план достаточен** - содержит всю необходимую информацию для старта
3. **LLM сам найдёт нужное** - имеет инструменты `Read`, `Glob`, `Grep` для чтения файлов
4. **Экономия контекста** - больше места для рабочего кода и анализа

**Правильный workflow для разных LLM:**

**Claude Code:**
1. Открыть новый чат
2. Прикрепить **ТОЛЬКО** `reasoning/0_REASONING_TASK.md`
3. Написать: "продолжи работу" или "проверь незавершённые сессии"
4. LLM автоматически найдёт и прочитает нужные файлы сессий

**ChatGPT / Claude Web / другие web-интерфейсы:**
1. Открыть новый чат
2. Прикрепить **ТОЛЬКО** `reasoning/0_REASONING_TASK.md`
3. Написать запрос с контекстом
4. LLM будет использовать мастер-план как основу

**API (Claude API / OpenAI API):**
```python
# Читать ТОЛЬКО мастер-план
with open("reasoning/0_REASONING_TASK.md") as f:
    master_plan = f.read()

messages = [
    {"role": "system", "content": master_plan},
    {"role": "user", "content": "продолжи работу"}
]
```

**Как LLM находит файлы сессий автоматически:**
- LLM читает правило "Фаза 0: Проверка незавершённых сессий"
- Использует `Glob` для поиска `reasoning/iterations/*.md`
- Читает последние файлы с помощью `Read`
- Продолжает работу с нужным контекстом
- **Вам НЕ нужно делать это вручную!**

## Метаинформация проекта
- **Платформа**: Kubernetes-ориентированная система для распределённого выполнения LLM
- **Архитектура**: OpenAI-совместимый API → NATS JetStream → Distributed LLaMA
- **Основной язык**: Go 1.21+
- **Оркестратор**: Kubernetes с Wrangler-контроллерами

## Основные компоненты системы

### 1. Custom Resources (CRD)
- `Session` - управление диспетчерами и пулами Dllama
- `Dllama` - топология distributed-llama для конкретной модели
- `Model` - загрузка, конвертация и управление артефактами моделей
- `Root` - координатор distributed-llama (Deployment)
- `Worker` - рабочие узлы StatefulSet
- `Ingress` - публичная точка входа и backend

### 2. Режимы работы бинарника (`cmd/operator`)
- `operator` - контроллеры и синхронизация с JetStream
- `backend/ingress` - OpenAI HTTP API, валидация токенов
- `dispatcher` - распределение запросов между воркерами
- `llm` - sidecar для dllama-api процессов

### 3. Внешние зависимости
- NATS JetStream (KV buckets, streams)
- Object Storage (S3/MinIO) для моделей
- Kubernetes CSI для PVC

## Цикл итеративной работы

### Фаза 0: Проверка незавершённых сессий (ОБЯЗАТЕЛЬНО!)
```yaml
КРИТИЧЕСКИ ВАЖНО: При каждом запуске работы ПЕРВЫМ ДЕЛОМ проверить наличие незавершённых сессий!

actions:
  - Проверить файлы в reasoning/iterations/ отсортированные по дате
  - Найти последнюю сессию и проверить её статус
  - Если статус != "Завершено ✅", ПРОДОЛЖИТЬ эту сессию
  - НЕ создавать новые отчёты/сессии если есть незавершённая работа
  - Прочитать план из незавершённой сессии
  - Продолжить с того места где остановились

outputs:
  - Решение: продолжить существующую сессию ИЛИ начать новую
  - Контекст незавершённой работы (если есть)
  - План действий для продолжения

ВАЖНО: Это предотвращает дублирование работы и потерю контекста!

context_management:
  КРИТИЧЕСКОЕ ПРАВИЛО - Управление контекстом LLM чата:
  - После завершения логического этапа работы (например, создание одного модуля)
  - При достижении ~50-60% использования token budget
  - После успешного коммита изменений
  - НЕМЕДЛЕННО предложить пользователю:
    1. Завершить текущий чат с LLM
    2. Начать новый чат, сославшись на этот файл
    3. Продолжить с обновлённым контекстом
  
  Причины:
  - Предотвращение переполнения контекста
  - Улучшение качества ответов LLM
  - Снижение латентности и стоимости запросов
  - Фокусировка на текущей задаче без "хвоста" истории
  
  Формулировка предложения:
  "Этап завершён успешно! Рекомендую завершить текущий чат и начать новый,
  чтобы не забивать контекст. При запуске нового чата я автоматически
  продолжу работу с обновлённым состоянием из файла отчёта."
```

### Фаза 1: Аудит и диагностика
```yaml
inputs:
  - Текущее состояние кодовой базы
  - Логи последних изменений
  - Issue tracker / известные проблемы
  
actions:
  - Анализ консистентности CRD ↔ контроллеры
  - Проверка работы с JetStream (buckets, streams)
  - Валидация масштабирования сессий
  - Инспекция health endpoints

outputs:
  - Список выявленных проблем
  - Гипотезы о причинах
  - План диагностики
```

### Фаза 2: Планирование исправлений
```yaml
inputs:
  - Результаты диагностики
  - Архитектурные ограничения
  - Приоритеты разработки

actions:
  - Формирование задач на итерацию
  - Оценка влияния изменений
  - Определение порядка выполнения
  - Подготовка тестовых сценариев

outputs:
  - Детальный план работ
  - Критерии успеха
  - Риски и митигации
```

### Фаза 3: Реализация
```yaml
inputs:
  - План работ
  - Существующий код
  - Тестовое окружение

actions:
  - Внесение кодовых правок
  - Обновление манифестов/чартов
  - Написание/обновление тестов
  - Форматирование и линтинг

outputs:
  - Измененные файлы
  - Результаты тестов
  - Документация изменений
```

### Фаза 4: Валидация
```yaml
inputs:
  - Внесённые изменения
  - Тестовые сценарии
  - Метрики производительности

actions:
  - Прогон unit-тестов
  - Интеграционное тестирование
  - Проверка регрессий
  - Анализ производительности

outputs:
  - Отчёт о тестировании
  - Метрики качества
  - Список найденных проблем
```

### Фаза 5: Документирование
```yaml
inputs:
  - Результаты итерации
  - Изменения в архитектуре
  - Новые best practices

actions:
  - Обновление README/docs
  - Актуализация architecture.svg
  - Создание changelog
  - Документирование решений

outputs:
  - Обновлённая документация
  - Отчёт об итерации
  - Рекомендации на будущее
```

### Фаза 6: Коммит изменений
```yaml
inputs:
  - Результаты валидации
  - Документация изменений
  - Проверка безопасности

prerequisites:
  - ✅ Все тесты проходят успешно
  - ✅ Проект собирается без ошибок
  - ✅ Тесты адекватно написаны и покрывают изменения
  - ✅ Отсутствуют приватные данные (токены, пароли, секреты)
  - ✅ Код отформатирован (go fmt)
  - ✅ Документация актуализирована

actions:
  - Проверка git status
  - Формирование атомарных коммитов
  - Написание информативных commit message
  - git commit с описанием изменений

commit_message_format: |
  <type>: <краткое описание>
  
  <детальное описание изменений>
  
  - Что было исправлено/добавлено
  - Почему это было необходимо
  - Какие проблемы решает
  
  Relates-to: #issue-number (если применимо)

commit_types:
  - fix: исправление бага
  - feat: новая функциональность
  - refactor: рефакторинг без изменения функциональности
  - test: добавление/изменение тестов
  - docs: изменения в документации
  - chore: вспомогательные изменения (CI, deps, etc)
  - perf: улучшение производительности

outputs:
  - Атомарные git commits
  - Чистая история изменений
  - Готовность к push/PR
```

## Ключевые области внимания

### NATS JetStream
- **ConversationKV**: TTL управление, префиксы `nats_ttl_`
- **Buckets**: `koldun_models`, `koldun_tokens`, синхронизация
- **Streams**: `KOLDUN_LLM_REQUESTS`, обработка backlog
- **Assignments**: распределение задач между воркерами

### Масштабирование
- Минимумы/максимумы для `Dllama`
- `ScaleDownIdleSeconds` тайминги
- Dispatcher очередь и requeue логика
- Автоскейлинг на основе метрик

### Управление моделями
- Скачивание через `download.py`
- Конвертация в GGUF формат
- Управление PVC и S3 storage
- Состояния `ModelStatus`

### Health & Monitoring
- `/healthz`, `/readyz` endpoints
- Реакция на потерю связи с NATS
- Heartbeat механизмы
- Метрики производительности

## Контрольные точки качества

### Перед началом итерации
- [ ] Актуальность git репозитория
- [ ] Чистота рабочего пространства
- [ ] Доступность зависимостей

### В процессе работы
- [ ] Атомарность изменений
- [ ] Соответствие код-стайлу
- [ ] Покрытие тестами
- [ ] Обратная совместимость

### После завершения
- [ ] Прохождение всех тестов
- [ ] Отсутствие регрессий
- [ ] Документирование изменений
- [ ] Готовность к деплою

## Инструменты и команды

### Разработка
```bash
go fmt ./... && gofmt -w .           # Форматирование
go test ./...                         # Unit тесты
go test -race ./...                   # Race condition проверка
go build ./cmd/operator               # Сборка бинарника
```

### Контейнеризация
```bash
skaffold build                        # Сборка образа
docker build -t koldun:dev .         # Локальная сборка
```

### Kubernetes
```bash
kubectl apply -k k8s/                 # Применение манифестов
helm upgrade koldun charts/koldun     # Обновление через Helm
kubectl logs -l app=koldun           # Просмотр логов
```

### NATS JetStream
```bash
nats kv ls                            # Список KV buckets
nats stream ls                        # Список streams
nats consumer info KOLDUN_LLM_REQUESTS  # Информация о consumer
```

## Метрики успеха итерации

1. **Функциональность**
   - Все тесты проходят успешно
   - Отсутствуют критические баги
   - API совместимость сохранена

2. **Производительность**
   - Latency < целевых значений
   - Throughput соответствует требованиям
   - Ресурсы используются эффективно

3. **Надёжность**
   - Graceful shutdown работает
   - Reconnect логика функционирует
   - Отказоустойчивость обеспечена

4. **Документация**
   - Код документирован
   - README актуален
   - Диаграммы соответствуют реальности

## Следующие шаги

1. Выполнить аудит текущего состояния
2. Определить приоритетные проблемы
3. Запустить первую итерацию отладки
4. Документировать результаты в `iterations/YYYY-MM-DD_HH-MM_session_N.md`

## Правила именования файлов итераций

Файлы сессий должны сохраняться в папке `iterations/` со следующим форматом имени:
- **Формат**: `YYYY-MM-DD_HH-MM_session_N.md`
- **Пример**: `2025-11-01_14-30_session_1.md`
- **Где**:
  - `YYYY-MM-DD` - дата проведения сессии
  - `HH-MM` - время начала сессии (24-часовой формат)
  - `N` - порядковый номер сессии в этот день

Это позволяет:
- Точно отслеживать хронологию работы
- Избегать конфликтов при множественных сессиях в день
- Легко находить конкретную сессию по времени